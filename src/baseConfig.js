// @ts-check

/**
 * @type {import('vitepress').UserConfig}
 */
const config = {
    cleanUrls: true,
    markdown: {
        lineNumbers: false,
        theme: 'css-variables',
    },
}

module.exports = config